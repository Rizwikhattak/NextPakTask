specVersion: "v1beta"
serviceId: "nextpaksolstask"
location: "us-central1"
schema:
  source: "./schema"
  datasource:
    postgresql:
      database: "fdcdb"
      cloudSql:
        instanceId: "nextpaksolstask-fdc"
      # schemaValidation: "COMPATIBLE"
connectorDirs: ["./connector"]
